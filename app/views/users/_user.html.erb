<li>
  <%= gravatar_for user, size: Settings.length.digit_50 %>
  <%= link_to user.name, user %>
  <% if is_correct_user_admin? @user %>
    | <%= link_to t(".delete"), user, method: :delete, data: {confirm: t(".is_sure")} %>
  <% end %>
</li>
